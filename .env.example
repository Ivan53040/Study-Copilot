# Optional overrides. Copy to .env (which is git-ignored).

# Path to the YAML config (defaults to ./config.yaml)
STUDY_COPILOT_CONFIG=config.yaml

# Cloud model keys (only used if models.cloud_fallback.enabled = true)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
